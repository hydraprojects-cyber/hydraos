namespace HydraOS.BIOS;

public static class BiosDiagnostics
{
    public static bool RunBasicChecks()
    {
        // Verifica se defaults carregaram
        // Verifica se há tema
        // Verifica se há utilizador base
        // Verifica se há modo de arranque
        // (No futuro) Verifica integridade do sistema

        return true; // Por agora assume que está tudo OK
    }
}
